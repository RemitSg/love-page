# Love Page

一个可以部署到 GitHub Pages 的静态网页礼物。

## 修改内容

- 编辑 `index.html` 里的文字。
- 编辑 `styles.css` 调整颜色和排版。
- 把照片放进仓库后，可以在 `index.html` 里添加 `<img>`。

## GitHub Pages

这个仓库使用 GitHub Pages 从 `main` 分支根目录部署。
